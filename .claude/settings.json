{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath // empty' | { read -r f; echo \"$f\" | grep -qE '\\.(jsx?|tsx?)$' && npx eslint --fix \"$f\"; } 2>/dev/null || true",
            "statusMessage": "ESLint fixing..."
          }
        ]
      }
    ]
  }
}
